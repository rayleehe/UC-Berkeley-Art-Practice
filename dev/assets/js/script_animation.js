// logo animation
$(window).scroll(function() {
  if ($(this).scrollTop() >150) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_21').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_21 img').height(),
    });
    var logo_height = $('#logo_21 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <150 && $(this).scrollTop() >145) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_20').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_20 img').height(),
    });
    var logo_height = $('#logo_20 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
          'margin-top': big_header_height - 10 - 10 ,
    });
  } else if ($(this).scrollTop() <145 && $(this).scrollTop() >140) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_19').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_19 img').height(),
    });
    var logo_height = $('#logo_19 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <140 && $(this).scrollTop() >135) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_18').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_18 img').height(),
    });
    var logo_height = $('#logo_18 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <130 && $(this).scrollTop() >125) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_17').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_17 img').height(),
    });
    var logo_height = $('#logo_17 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <125 && $(this).scrollTop() >120) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_16').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_16 img').height(),
    });
    var logo_height = $('#logo_16 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <120 && $(this).scrollTop() >115) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_15').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_15 img').height(),
    });
    var logo_height = $('#logo_15 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <115 && $(this).scrollTop() >110) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_14').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_14 img').height(),
    });
    var logo_height = $('#logo_14 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <110 && $(this).scrollTop() >100) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_13').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_13 img').height(),
    });
    var logo_height = $('#logo_13 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <100 && $(this).scrollTop() >90) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_12').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_12 img').height(),
    });
    var logo_height = $('#logo_12 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <90 && $(this).scrollTop() >80) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_11').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_11 img').height(),
    });
    var logo_height = $('#logo_11 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <80 && $(this).scrollTop() >70) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_10').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_10 img').height(),
    });
    var logo_height = $('#logo_10 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <60 && $(this).scrollTop() >55) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_09').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_09 img').height(),
    });
    var logo_height = $('#logo_09 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <50 && $(this).scrollTop() >40) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_08').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_08 img').height(),
    });
    var logo_height = $('#logo_08 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <35 && $(this).scrollTop() >30) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_07').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_07 img').height(),
    });
    var logo_height = $('#logo_07 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <25 && $(this).scrollTop() >20) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_06').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_06 img').height(),
    });
    var logo_height = $('#logo_06 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <20 && $(this).scrollTop() >15) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_05').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_05 img').height(),
    });
    var logo_height = $('#logo_05 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <15 && $(this).scrollTop() >12) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_04').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_04 img').height(),
    });
    var logo_height = $('#logo_04 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <10 && $(this).scrollTop() >7) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_03').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_03 img').height(),
    });
    var logo_height = $('#logo_03 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <7 && $(this).scrollTop() >5) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_02').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_02 img').height(),
    });
    var logo_height = $('#logo_02 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10 ,
    });
  } else if ($(this).scrollTop() <5) {
    $('.top-header').css('visibility', 'hidden');
    $('#logo_01').css('visibility', 'visible');
    $('.big-header').css({
        position: 'fixed',
        'margin-top': $('#logo_01 img').height(),
    });
    var logo_height = $('#logo_01 img').height();
    var big_header_height = $('.big-header').height();
    $('#wrapper-img').css({
        top: 0,
         'margin-top': big_header_height - 10,
    });
  }
});


// const dq = window.matchMedia( "(min-width: 800px)" );

var big_header_height = $('.big-header').height();
var header_height = $('.header').height();
var header_line_height = $('.header-line2').height();
if (dq.matches) {
  $('.dropdown-content').css({
    'margin-top': header_height + header_line_height
  });
}

// fixed header
var fixmeTop = $('#logo_01').offset().top;
var stickyHeaderTop = $('.big-header').offset().top;
$(window).scroll(function() {
    var currentScroll = $(window).scrollTop();
    var distance = (stickyHeaderTop - currentScroll);
    var logo_21_height = $('#logo_21 img').height();
    var big_header_height = $('.big-header').height();
    var header_height = $('.header').height();
    var header_line_height = $('.header-line2').height();
    if (distance = logo_21_height) {
        // $("#wrapper-img img").attr("src","assets/img/logo_00.jpg");
        // $('#wrapper-img').css({
        //     top: 0,
        //     'margin-top': logo_21_height + big_header_height ,
        // });
        // $('.big-header').css({
        //     position: 'fixed',
        //     'margin-top': logo_21_height
        // });
        $('.dropdown-content').css({
             'margin-top': big_header_height - (100 * (1/10)),
        });
        if (dq.matches) {
          $('.big-header').css({
            'margin-top': '0',
            position: 'fixed'
          });
          $('.dropdown-content').css({
            'margin-top': header_height + header_line_height
          });
      }
    }
});

$(window).scroll(function() {
    var currentScroll = $(window).scrollTop();
    var distance = (stickyHeaderTop - currentScroll);
    var logo_21_height = $('#logo_21 img').height();
    var big_header_height = $('.big-header').height();
    var header_height = $('.header').height();
    var header_line_height = $('.header-line2').height();
    var logo_01_height = $('#logo_01 img').height();
    if (distance >= logo_21_height) {
      // $("#wrapper-img img").attr("src","assets/img/logo_00.jpg");
      // $('#wrapper-img').css({
      //     'margin-top': '0'
      // });
      // $('.big-header').css({
      //     position: 'static',
      //     'margin-top': logo_01_height,
      // });
      $('.dropdown-content').css({
          'padding-top': '0'
      });
      if (dq.matches) {
        $('.dropdown-content').css({
          'margin-top': header_height + header_line_height,
          'width': '30vw'
        });
        $('.big-header').css({
            position: 'fixed',
            'margin-top': '0'
        });
    }
  }
});
